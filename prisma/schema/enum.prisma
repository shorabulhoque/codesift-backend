enum UserRole {
    ADMIN
    RECRUITER
    CANDIDATE
}

enum UserStatus {
    ACTIVE
    BLOCKED
    PENDING
}

enum SubscriptionPlan {
    FREE
    PREMIUM
}

enum Difficulty {
    EASY
    MEDIUM
    HARD
}

enum SubmissionStatus {
    PENDING
    PASSED
    FAILED
    ERROR
}
