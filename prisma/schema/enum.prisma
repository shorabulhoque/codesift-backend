enum UserRole {
    ADMIN
    RECRUITER
    CANDIDATE
}

enum UserStatus {
    ACTIVE
    BLOCKED
    PENDING
}

enum RecruiterVerificationStatus {
    PENDING
    APPROVED
    REJECTED
}

enum AuthProvider {
    CREDENTIALS
    GOOGLE
}

enum SubscriptionPlan {
    FREE
    PREMIUM
}

enum Difficulty {
    EASY
    MEDIUM
    HARD
}

enum SubmissionStatus {
    PENDING
    PASSED
    FAILED
    ERROR
}

enum ApplicationStatus {
    SUBMITTED
    UNDER_REVIEW
    SHORTLISTED
    INTERVIEW_SCHEDULED
    REJECTED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
    CANCELLED
}

enum PaymentGateway {
    STRIPE
}
