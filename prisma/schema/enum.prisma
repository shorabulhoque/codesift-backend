enum UserRole {
    ADMIN
    RECRUITER
    CANDIDATE
}

enum UserStatus {
    ACTIVE
    BLOCKED
    PENDING
}

enum RecruiterVerificationStatus {
    PENDING
    APPROVED
    REJECTED
}

enum AuthProvider {
    CREDENTIALS
    GOOGLE
}

enum SubscriptionPlan {
    FREE
    PREMIUM
}

enum Difficulty {
    EASY
    MEDIUM
    HARD
}

enum SubmissionStatus {
    PENDING
    PASSED
    FAILED
    ERROR
}
